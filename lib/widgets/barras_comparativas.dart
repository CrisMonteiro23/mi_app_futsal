import 'package:flutter/material.dart';

class BarrasComparativas extends StatelessWidget {
  const BarrasComparativas({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Gráfico en construcción'),
    );
  }
}
