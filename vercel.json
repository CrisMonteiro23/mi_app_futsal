{
  "builds": [
    {
      "src": "index.html",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build/web"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "flutter build web --release",
  "installCommand": "curl -O https://storage.googleapis.com/flutter_infra_release/releases/stable/windows/flutter_windows_3.22.2-stable.zip && unzip flutter_windows_3.22.2-stable.zip && rm flutter_windows_3.22.2-stable.zip && export PATH=\"$(pwd)/flutter/bin:$PATH\""
}
